package internal

// internal components that are used between various plugin packages, and
// gatekeeper core. Anything that is a shared component across the gatekeeper
// ecosystem but shouldn't be accessible by public plugins and/or doesn't belong
// in the `gatekeeper` core package should go in here.
